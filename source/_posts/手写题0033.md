---
title: "手写题0033"
---
## 防抖与节流

防抖：类似于王者荣耀的游戏回城，一直点是没有用的，**只执行最后一次**。
节流：类似于王者荣耀的技能冷却，点了之后**一段时间再次点击是无效的**。

**防抖（debounce）**

```javascript
//靠定时器实现的，单位时间内只执行最后一次
function debounce(func,delay){
  let timer=null
  return function(){
    let context=this
    clearTimeout(timer)//在延迟的任务之前进行
     timer=settimeout(()=>{
      func.call(context)
    },dalay)
  }
}
```

**节流**

```javascript
    function throttle(func, delay) {
            let timer = null
            return function () {
                if (timer === null)   return
                else {
                    timer = setTimeout(() => {
                        func();
                        timer = null//在延迟的任务之后进行，直接清空timer
                    }, dalay)
                }
            }
        }
```
## promise
promise**本身是同步的**而他的then和catch才是异步的微任务，同步任务在执行栈中直接执行，而后看微任务,到了执行时机时进入执行栈，接着执行settimeout这种宏任务
### 简单的promise
```javascript
class commitment{
  static pending="待定"
  static fullfilled="成功"
  static rejected="拒绝"
  constructor(func){
    this.status=pending
    this.result=result
}
resolve(result){
  if(this.status===commitment.pending)
  {
    this.status=commitment.fulliflled
    this.result=result
  }
reject(result){
    if(this.status===commitment.pending)
  {
    this.status=commitment.rejected
  }
}
}}
```
### promise.all
```javascript
static all(array) {
                let result = []
                let index = 0
                return new Mypromise((resolve, reject) => {
                    const addData = (key, value) => {
                        result[key] = value
                        index++
                        if (index === array.length)//index确保array中的各种元素都执行完成之后再进行下一步操作
                        {
                            resolove(result)
                        }
                    }
                    if (array.isarray === false) {
                        console.log("不是数组")
                    }
                    else {
                        for (let i = 0; i < array.length; i++) {
                            let current = array[i]
                            if (current instanceof Mypromise) {
                                current.then(value => addData(i, value), reason => reject(reason))
                            }
                            else {
                                addDate(i, array[i])
                            }
                        }
                    }
                })
            }
```
